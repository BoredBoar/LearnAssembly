; -----------------------------------------------------------------------------
; Lesson 02 - Hello, World          >>> THIS IS THE ONLY FILE YOU EDIT <<<
;
; YOUR TASK
;   Put the value 42 into the register named OUTPUT.
;
;   OUTPUT is another name for r16. One instruction is enough.
;
; HOW YOU KNOW IT WORKED
;   Build and flash with:   ./avr 2
;     LED on, steady  ->  correct, OUTPUT holds 42
;     LED blinking    ->  wrong, OUTPUT holds something else
;
; A HINT, IF YOU NEED ONE
;   Lesson 01 used the instruction that loads a constant into a register three
;   times in a row. Look at how it filled CNT_LO, and note the rule that comes
;   with it: that instruction only works on r16-r31, which is why OUTPUT is r16.
; -----------------------------------------------------------------------------


    ; ================= write your answer below this line =================



    ; ================= write your answer above this line =================
